selectors:
  - name: energia
    definition:
      union:
        - method: path
          value: models/staging/solar_weather_project
          children: True
  - name: nhl
    definition:
      union:
        - method: path
          value: models/staging/nhl
          children: True
  - name: vide
    definition:
      union:
        - method: path
          value: models/staging/vide
          children: True
