selectors:
  - name: energia
    definition:
      method: path
      value: models/staging/solar_weather_project
      children: True
  - name: nhl
    definition:
      method: path
      value: models/staging/nhl
      children: True
  - name: livros
    definition:
      method: path
      value: models/staging/livros
      children: True
  - name: inflation
    definition:
      method: path
      value: models/staging/inflation
      children: True
